# Analytics

Analytics conventions:

- Log meaningful feature events only
- Use structured event names (`feature_action_result`)
- Avoid sending sensitive payloads
- Keep event schema documented when introducing new events
