# Notifications

Notification stack:

- `flutter_local_notifications` for local notifications
- `timezone` for scheduled reminders
- Firebase Messaging dependency available for push workflows

Initialize local notifications in `NotificationService.initialize()`.
