# Imagery

Imagery strategy:

- Use vector/Lottie placeholders for onboarding and empty states
- Keep splash/logo assets optimized for fast startup
- Use `AppImage` for remote images with placeholder + error fallback
