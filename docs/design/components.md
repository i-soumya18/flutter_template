# Components

Core reusable components:

- `AppButton` (primary, secondary, ghost, destructive)
- `AppTextField` (regular, search, password)
- `AppLoadingWidget` (spinner, shimmer)
- `AppErrorView`, `AppEmptyView`
- `AsyncValueWidget<T>`
- Bottom sheet, dialog, snackbar, image wrapper
