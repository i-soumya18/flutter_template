# Folder Structure

Key structure:

- `lib/main.dart`, `lib/bootstrap.dart`, `lib/app.dart`
- `lib/core/{config,constants,errors,extensions,network,router,services,theme,utils,widgets}`
- `lib/features/{splash,onboarding,auth,dashboard,profile,settings}`
- `docs/` for architecture, design, screens, infrastructure, and playbooks
